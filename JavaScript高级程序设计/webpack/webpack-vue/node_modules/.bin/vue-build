#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../vue-cli/bin/vue-build" "$@"
  ret=$?
else 
  node  "$basedir/../vue-cli/bin/vue-build" "$@"
  ret=$?
fi
exit $ret
